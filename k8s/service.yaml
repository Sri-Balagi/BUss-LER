apiVersion: v1
kind: Service
metadata:
  name: bizos-api-svc
  namespace: bizos-prod
spec:
  selector:
    app: bizos
    component: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
